import { Icon, type IconProps } from "./icon";

/* lucide: check */
export const CheckIcon = (props: IconProps) => {
  return (
    <Icon {...props}>
      <path d="M20 6 9 17l-5-5" />
    </Icon>
  );
};
